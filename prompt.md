# 项目目标
为一名计算机相关专业大二学生创建一个数据结构交互式可视化学习项目。学习者特点：
- 第一次系统学习数据结构
- Python 基础薄弱（能看懂语法，写起来不熟练）
- 视觉化 + 步进式学习者：不喜欢连续动画（看不清在发生什么），喜欢"点一下，动一步"的节奏，能停下来盯着每一步看
- 学习目标：理解每个数据结构的"是什么、为什么、怎么用"，应付课程考试和编程作业

# 核心设计原则（重要，请严格遵守）
1. **步进式交互优先**：所有可视化必须是"点击下一步前进一步"的形式，绝不做自动播放的连续动画
2. **每一步都有解释**：每点一次"下一步"，界面上要更新一段文字，说明：
   - 当前在做什么操作（例如"正在将 7 上浮到正确位置"）
   - 为什么这么做（例如"7 比父节点 5 大，违反最大堆性质"）
   - 下一步会做什么（例如"接下来比较 7 和新的父节点 9"）
3. **可以后退**：除了"下一步"，还要有"上一步"按钮，能回看
4. **状态高亮**：当前操作涉及的元素用醒目颜色（如红色），刚操作过的用黄色，无关元素保持灰色
5. **数据和视图同步**：例如堆，左边画树形结构，右边显示数组表示，两边对应位置高亮要联动

# 技术栈
- **数据结构实现**：纯 Python（在 .py 文件中），用类实现，方便她对照教材学
- **可视化界面**：HTML + 原生 JavaScript + SVG/Canvas（不用框架，降低复杂度）
- **两者关系**：Python 是"真实实现"（教学用），HTML 是"演示界面"（理解用）
  - Python 端：完整实现每种数据结构，带详细中文注释，能独立运行并打印操作过程
  - HTML 端：用 JS 重新实现一遍可视化逻辑（这部分她不需要看懂 JS 代码，只用界面）
  - 两边算法逻辑必须一致

# 目录结构
data-structures-tutorial/
├── README.md                     # 总览 + 如何使用
├── index.html                    # 主页：所有数据结构的入口
├── shared/
│   ├── style.css                 # 统一样式
│   └── utils.js                  # 通用工具：步骤管理、按钮组件等
├── 00_python_basics/
│   ├── README.md
│   └── *.py                      # 纯 Python 速成
├── 01_array_and_list/
│   ├── README.md                 # 中文讲解：原理 + 类比 + 考点
│   ├── implementation.py         # Python 实现，带注释
│   ├── visualization.html        # 可视化界面
│   └── visualization.js          # 可视化逻辑
├── 02_linked_list/
├── 03_stack/
├── 04_queue/
├── 05_recursion/
├── 06_tree_basics/
├── 07_binary_search_tree/
├── 08_heap/
├── 09_hash_table/
├── 10_graph/
└── 11_sorting_algorithms/

# 交互界面的标准布局（每个 visualization.html 都遵循）
┌─────────────────────────────────────────────┐
│  标题：XX 数据结构 - XX 操作演示              │
├─────────────────────────────────────────────┤
│                                             │
│         [主可视化区域：SVG 绘制]             │
│                                             │
│                                             │
├─────────────────────────────────────────────┤
│  当前步骤说明：                              │
│  "正在比较节点 7 和父节点 5..."              │
│                                             │
│  下一步将：                                  │
│  "因为 7 > 5，交换两者位置"                  │
├─────────────────────────────────────────────┤
│  操作选择：[插入] [删除] [查找]              │
│  输入数据：[___] [执行]                      │
│  [← 上一步] [步骤 3/12] [下一步 →] [重置]    │
└─────────────────────────────────────────────┘

# 步进逻辑实现要求
每个可视化要用"状态机"思路实现，不要用 setTimeout 串联动画：
- 维护一个 steps 数组，每个元素是一个"快照"（snapshot），包含：
  - 当前数据结构的完整状态（数组/树结构等）
  - 高亮信息（哪些节点该亮哪种颜色）
  - 文字说明（当前在做什么、为什么、下一步是什么）
- 点"下一步"就 currentStep++，重新渲染
- 点"上一步"就 currentStep--，重新渲染
- 这样可以任意跳转，绝不卡顿

# 每个章节必须有的演示
重点章节的演示场景（每个都要做成步进式）：

## 链表（02）
- 头部插入、尾部插入、中间插入（重点画指针重连）
- 删除节点（重点画指针绕过）
- 反转链表（经典考题）

## 栈（03）
- push / pop 基本操作
- 应用演示：括号匹配（逐字符扫描，栈的变化）
- 应用演示：中缀转后缀表达式

## 队列（04）
- 普通队列 enqueue / dequeue
- 循环队列（重点画首尾指针的移动，理解"假溢出"）

## 递归（05）
- 调用栈可视化：以阶乘和斐波那契为例
- 每点"下一步"就压栈或弹栈一次
- 同时显示当前的递归树

## 树（06, 07）
- 三种遍历：每点一次下一步，访问指针移动到下一个节点，并把节点编号加入访问序列
- BST 插入：从根开始比较，每次比较高亮一次
- BST 删除：特别详细地演示"删除有两个孩子的节点"——找后继 → 替换 → 删除后继

## 堆（08）⭐ 这是你举的例子，做最精细
- **插入**：放到末尾 → 上浮（每次和父节点比较 → 决定是否交换 → 移动），每一步都停下
- **删除堆顶**：用末尾替换 → 下沉（每次和较大的孩子比较 → 决定是否交换），每一步都停下
- **双视图**：左边树形图，右边数组表示，对应位置同步高亮
- **建堆**：Floyd 建堆法，从最后一个非叶子节点开始下沉

## 哈希表（09）
- 插入时演示哈希函数计算 → 定位桶 → 处理冲突（链地址法和开放寻址法各做一个）
- 扩容（rehash）过程

## 图（10）
- BFS：每点一步，从队列取出一个节点 → 访问 → 把邻居加入队列，同步高亮队列变化
- DFS：用栈实现的版本，每点一步压栈/弹栈一次
- Dijkstra 最短路径（如果她课程要求）

## 排序（11）
- 至少：冒泡、选择、插入、归并、快排
- 每次比较和交换都是一步
- 用柱状图表示数组，颜色区分"已排序区"、"当前比较"、"待排序区"

# Python 实现要求（每个 implementation.py）
- 用类实现，方法命名贴近教材（push/pop/enqueue/dequeue/insert/delete 等）
- 每个方法上方注释：作用、参数、返回值、时间复杂度
- 关键步骤用 print 输出当前状态（学生在终端跑也能看到过程）
- 文件底部有 `if __name__ == "__main__":` 完整演示
- 避免用 Python 现成的高级容器（如 collections.deque、heapq），手写实现
- 但在 README 中对比："我们手写的 vs Python 自带的"

# README.md 每章必含内容
1. 生活化类比（例："栈就像羽毛球筒"）
2. 核心操作及时间复杂度表
3. 应用场景（结合课程考点）
4. 常见考点和易错点（特别是面试/考试常问的）
5. 学习检查清单（"看完这章你应该能回答..."）
6. 如何使用本章：先看 README → 跑 Python 代码 → 打开 HTML 玩交互演示

# 主页 index.html
- 一个简洁的导航页，列出所有章节
- 每个章节卡片显示：编号、名称、难度、预计学习时间、是否完成（用 localStorage 记录）
- 推荐学习顺序的箭头/路线图

# 代码规范
- 变量名函数名英文，注释和文字说明全中文
- 每个文件开头中文说明：本文件做什么、对应教材哪一章、预计学习时长
- JS 代码尽量平铺直叙，不要用箭头函数嵌套、解构等"花哨写法"——即使她以后想看也能看懂
- 配色：用柔和但区分度高的颜色，不要用过于鲜艳的色系（红/黄/绿/灰 四色已够用）

# 输出顺序（请按此顺序生成，方便我审阅）
1. 先生成项目总览：README.md + index.html + shared/
2. 然后生成 00_python_basics（Python 速成）
3. 然后按章节顺序生成，从 01_array_and_list 开始
4. **每生成完一个章节后停下**，简述：
   - 这章的设计思路
   - 可视化的难点和你是怎么解决的
   - 等我说"继续"再做下一章

# 不要做的事
- ❌ 不要做自动播放的连续动画
- ❌ 不要用 setTimeout/setInterval 来控制可视化推进——必须由用户点击驱动
- ❌ 不要一次性生成所有文件
- ❌ 不要用 React/Vue 等框架（增加复杂度，她看不懂）
- ❌ 不要写成"完美工业级代码"，要写成"教学代码"——清晰胜于优雅
- ❌ 不要跳过任何中间步骤——初学者最需要看清"每一小步"